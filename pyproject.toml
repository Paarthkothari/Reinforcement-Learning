[project]
name = "finance-ops-openenv"
version = "1.0.0"
description = "OpenEnv-compatible finance operations environment for invoice extraction, validation, and PO reconciliation."
requires-python = ">=3.10"
dependencies = [
  "fastapi==0.115.12",
  "gradio==5.30.0",
  "uvicorn[standard]==0.34.0",
  "pydantic==2.11.3",
  "openai>=2.7.2",
  "openenv-core>=0.2.0",
]

[project.scripts]
server = "server.app:main"

[tool.setuptools]
include-package-data = true
packages = ["env", "env.tasks", "server"]
py-modules = ["app", "inference", "train_loop", "analyze_training", "__init__"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
